---
title: 常见问题
---

# 常见问题

## 哪些事件需要手动订阅

- `窗口即将关闭`
- `即将导航`
- `请求新窗口`
- `下载开始`
- `文件拖入`

## 可拦截事件怎么返回

- 返回 `1`：拦截
- 返回 `0`：放行

## IPC 推荐怎么写

推荐对象方法回调：

```wsv
Jade.置IPC频道回调 (本对象, "ipcChannelMessageCallback")
```

## 异步对话框需要手动释放吗

不需要。

## 托盘事件会自动订阅吗

会。`JadeView_托盘.创建()` 会自动订阅托盘事件。
