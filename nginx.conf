server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    charset utf-8;
    autoindex off;

    # 容器监听 80，TLS 由前置反代(宝塔)终结。nginx 自动补尾斜杠的 301 默认会生成
    # 绝对地址且用自身 http scheme → 把 https 降级成 http（Algolia 爬虫视作“跨域重定向”
    # 而拒爬，对 SEO/用户也有害）。改为相对重定向，scheme 交由客户端按原始 https 解析。
    absolute_redirect off;
    port_in_redirect off;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json image/svg+xml;

    # 发行版本快照：内容随每次部署 / 定时工作流更新，禁止长缓存，保证用户拿到最新数据
    location = /releases/data.json {
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    # 带内容指纹的静态资源：长期不可变缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # SPA 路由：dumi 为客户端路由（/docs/spec、/docs/api/xxx、/releases、/showcase 等均为前端路由），
    #   未命中真实文件时统一回退 index.html（以 200 返回，由前端路由接管）。
    # ⚠️ 不要用 `$uri/`：它对已存在的同名目录会 301 补尾斜杠（/docs/spec/quickstart → …/quickstart/），
    #    而 dumi 客户端路由又把尾斜杠去掉（…/quickstart/ → …/quickstart）→ 形成 server↔client 重定向环，
    #    Algolia 爬虫判定 "JavaScript redirect" 拒抓、整站 0 records。
    #    改用 `$uri/index.html` 直接以 200 服务该路由 HTML（无 301），最后回退 SPA 壳。
    location / {
        try_files $uri $uri/index.html /index.html;
        # index.html 不缓存：发版后立即生效
        add_header Cache-Control "no-cache";
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
}
